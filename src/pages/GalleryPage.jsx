import React from "react";
import { Typography, Box } from "@mui/material";
import "../PieChart.css";

const GalleryPage = () => (
  <Box sx={{ textAlign: "center", marginTop: "20vh" }}>
    <Typography variant="h4">This is Gallery Page</Typography>
  </Box>
);

export default GalleryPage;

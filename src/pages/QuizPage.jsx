import React from "react";
import { Typography, Box } from "@mui/material";
import "../PieChart.css";

const QuizPage = () => (
  <Box sx={{ textAlign: "center", marginTop: "20vh" }}>
    <Typography variant="h4">This is Quiz Page</Typography>
  </Box>
);

export default QuizPage;
